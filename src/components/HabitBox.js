import styled from "styled-components";

export default function HabitBox() {
    return(
        <BoxStyle>

        </BoxStyle>
    );
}

const BoxStyle = styled.div`
    width: 90vw;
    height: 90px;
    background: #FFFFFF;
    border-radius: 5px;

    margin-bottom: 10px;
`